create table task_4_foreign_0.entity_table
(
    id bigserial primary key,
    name varchar(128) not null;
);

create table task_4_foreign_1.entity_table
(
    id bigserial primary key,
    name varchar(128) not null;
);

create table task_4_foreign_2.entity_table
(
    id bigserial primary key,
    name varchar(128) not null;
);